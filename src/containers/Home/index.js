import React from 'react';

const Home=()=>{
    return <div>
        its  homed
        <button onClick={()=>{alert('click')}}>
            click me!
        </button>
    </div>
}
export default Home;
